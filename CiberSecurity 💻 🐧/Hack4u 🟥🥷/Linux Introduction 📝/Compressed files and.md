# Commands
There are tons of compression types out there, each with its own extraction command, but `7z` is a life-saver that handles pretty much all of them

Core syntax:
```
7z <comand/option> <filename>
```

Important commands/options
- `l`: list the content of the file
- `x`: to extract the information of the file